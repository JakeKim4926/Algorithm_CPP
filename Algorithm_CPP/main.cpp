#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

void IOFaster() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);
}

int main() {
	IOFaster();

	int node, edge = 0;
	bool result = false;
	cin >> node >> edge;


	return 0;
}