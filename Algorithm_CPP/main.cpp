#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>

using namespace std;

void IOFaster() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);
}

int main() {
	IOFaster();

	int N;
	cin >> N;


	return 0;
}